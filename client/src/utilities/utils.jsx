/**
 * Utility Fucntions
 */

/**
 * R2D (Round to 2 Decimals)
 *
 * Returns a value rounded to 2 decimal places.
 *
 * @param value The value to be rounded
 * @returns {number} The rounded value
 */
export function r2d(value) {
    return value.toFixed(2);
}